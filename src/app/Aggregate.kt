package app

fun main() {
    val numbers = (1..10).toList()

    println(numbers)
    println(numbers.min())
    println(numbers.max())
    println(numbers.average())
    println(numbers.sum())
    println(numbers.count())
}